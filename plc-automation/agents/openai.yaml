interface:
  display_name: "Industrial Automation"
  short_description: "PLC, HMI, SCADA, safety, commissioning"
  default_prompt: "Use $plc-automation to design an automation system from scope through commissioning before generating PLC code."

policy:
  allow_implicit_invocation: true
